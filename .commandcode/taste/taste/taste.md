# Taste
- Never uses em dashes (:) in UI copy or any user-facing text : uses a hyphen, comma, or restructured sentence instead. Confidence: 0.95
- Prefers full codebase reviews/analyses to be written out to a project file (e.g., `.agent/output.txt`) containing the complete analysis plus a prioritized roadmap of what to build next, rather than delivered only as a chat summary. Confidence: 0.95
- Wants a persistent always-loaded agent contract file (e.g., `AGENTS.md`) at the repo root that agents read at the start of every prompt : recording non-negotiable rules, codebase conventions, and standing decisions : mirroring the convention used in sibling projects. Confidence: 0.9
- Prefers keeping the project minimal and self-contained over pulling in external systems (e.g., sibling IAM/UI libraries) unless there's a clear, low-risk win : in a final-year project context, favors "keep it minimal" and explicitly records integration decisions in the roadmap file rather than leaving them open. Confidence: 0.75
- Wants the project's agent docs (`.agent/output.txt`, `AGENTS.md`) kept continuously in sync with the actual codebase state : verified against the code (git log, greps, file listings) before calling them current, with version banners bumped, completed items marked `[DONE]`, and spec items that were never implemented flagged honestly as gaps rather than implied to exist. Confidence: 0.85
- When an integration option is abandoned, prefers scrubbing all mentions of it from the docs entirely (no lingering "don't use X" warnings or rationale), while genuinely deferred alternatives are marked as on hold / "awaiting" with a pointer to the decision record : docs should reflect only the current decision state. Confidence: 0.7
- Wants the codebase snapshot regenerated (`node gen-snapshot.js`) as part of the pre-commit/ship workflow, so the committed state reflects the latest code (the snapshot itself is a gitignored local artifact, regenerated on demand). Confidence: 0.85
- Prefers committing changes and pushing directly to the `main` branch : no feature-branch/PR ceremony for routine changes. Confidence: 0.85
- Wants throwaway scratch/debug scripts deleted before committing rather than left as untracked clutter or included in the commit; keeps the repo free of one-off scaffolding. Confidence: 0.7
- Wants dead code fully removed, not just deprecated: when one component supersedes another (e.g., `SiteNav` replaces `Topbar`), the obsolete component file is deleted and every stale reference/comment naming it (in source and comments) is updated or purged, leaving no zombie code or misleading doc mentions. Confidence: 0.75
- Wants hero/landing content laid out as one cleanly aligned column: the intro paragraph and the stat block share a single consistent max-width so the whole centered column reads as one aligned unit, and stat cards stretch to equal height (`h-full`) so bottom-aligned caption/source lines line up even when labels wrap unevenly. Confidence: 0.7
- Uses conventional-commit style messages (`feat:`/`chore:`) with a short summary, a bulleted body describing each change, and a `Co-authored-by: CommandCodeBot` trailer. Confidence: 0.6
- Wants the dependency tree kept lean during a library migration: keep only the packages that are actually used and safely delete any that are useless, rather than leaving stale/unused dependencies installed. Confidence: 0.7
- Treats the in-house `@arcevo/facet-*` packages (facet being the successor to arc-ui) as the single canonical UI foundation and wants the whole client UI rebuilt on them, replacing older-generation/legacy components : including fully utilizing `facet-layout` and purging the legacy `components/ui` directory entirely once the migration is finished (Welcome, RIASEC, Personality, Results still to port). Emphasizes building pages "well on our components": even simple primitives (Checkbox, Input, Label, Button, Card) and page chrome (LandingLayout) should come from facet rather than hand-rolled HTML. Confidence: 0.85
- Wants the agent to actively hunt for "invisible" architectural gaps beyond the obvious ones : silent divergences between the code and the documented methodology (e.g., fake data injected at full weight, stored-but-never-consumed fields, hardcoded constants that make rejection paths untestable, side-effectful GETs, spec'd fields nothing collects) : and group them under methodology integrity/research-validity as first-class P0 concerns, not operational polish. Explicitly expects no visible *or* hidden bugs (e.g., stale-closure navigation races, async state-timing bugs) left in the codebase. Confidence: 0.85
- Wants execution ordered by dependencies and explicitly sequenced (e.g., "P0-1 → P0-2 → …" with items that must land first called out, methodology decisions pinned before seeding, schema+engine treated as one migration unit), not executed in parallel or in arbitrary order. Confidence: 0.75
- Prefers the agent to move straight from reading the project docs into executing the prioritized roadmap ("start executing on priority") rather than re-analyzing or pausing for further confirmation. Confidence: 0.75
- Wants the architectural/spec-compliance gaps (e.g., versioned catalog tables, engine purity/seams, audit-trail version linkage) identified against the spec and closed before moving to the operational priority list : treats "getting the architecture right first" as an explicit gate before executing on features : including explicitly getting the architecture sound before infusing tests. Confidence: 0.9
- Defers writing the final-year thesis Chapter 3 (materials & methods) until the full software rebuild is complete : the StreamWise chapters 1–2 rewrite in the repo root is the current thesis content, and Chapter 3 must describe the finished implementation. Confidence: 0.8
- Keeps auth self-contained (internal JWT + local role/requireRole RBAC) for now; defers migrating to `@arcevo/facet-auth`/`facet-sdk` (arc-id identity) until after the project thesis defense and only once arc-id is confirmed safe/stable for production integration (it is still in development) : revisit data migration ease when arc-id settles as the canonical identity. Confidence: 0.8
- Verify that `@arcevo/facet-components` actually has an Alert component before assuming facet "ships no Alert" and hand-rolling a custom `@/components/Alert.tsx` : the user expects facet to be used for alerts rather than a workaround custom primitive. Confidence: 0.6
- After an IDE crash, expects the agent to recover project state from the persistent priority file (`.agent/priority.txt`) and report what is done vs. what is left, resuming from the recorded plan rather than treating the session as a fresh start. Confidence: 0.8
- Wants client-side forms (academic scores, survey/assessment entry) to autosave drafts locally by default so a browser refresh or downtime does not lose a user's in-progress entries : and specifically to avoid creating duplicate DB rows on resubmit once the user finally triggers the save. Confidence: 0.7
- Expects the topbar/navbar to use the sticky `pill` variant of `@arcevo/facet-components` Navbar matching the facet landing page (not a variant that scrolls/moves), and the avatar dropdown styling (name + actions) to be cleaned up so it is not visually rough. Confidence: 0.85
- Wants the topbar to be minimal and consistent across all routes: no navigation links in it (brand + actions only), a fixed consistent width (not varying per page), and sticky so it never moves with the body : long content in the avatar dropdown (e.g., emails) should be truncated/styled so the panel doesn't look stretched or odd. Explicitly wants the *same* topbar on the landing page as everywhere else ("same topbar in our landing home page everywhere... be consistent") — one shared navbar rendered globally so no page renders a second/different nav and the chrome never varies by route. Confidence: 0.9
- Wants the audit/ship pass to also scrub stale schema models and stale data anywhere in the system (not just docs), in addition to keeping documentation and agents synced and deleting unnecessary files : treat "no stale models/data anywhere" as part of a clean, consistent final state. Confidence: 0.65
- After an IDE crash, expects the repo to be scanned for zero-byte/empty ("null") files left by the crash : verifying none exist (or fixing any found) as an explicit part of recovery, not just resuming work from the priority file. Confidence: 0.7
- When diagnosing reported bugs, expects root cause to be verified against the live system and real data (calling the running API with real tokens, querying the DB for actual values, testing endpoints with curl) before concluding : not static code-reading alone. Confidence: 0.7
- When a displayed metric looks broken but the underlying engine/methodology is test-pinned and correct, prefers fixing the presentation/interpretation layer (scale-appropriate thresholds, honest labels, explanatory copy) rather than altering the pinned engine math. Confidence: 0.6
- Development environment is Windows (cmd/PowerShell): Unix-only shell idioms (`find`, `tail`, `head`, `> /dev/null`) fail and should be replaced with PowerShell equivalents or plain `pnpm` commands. Confidence: 0.8
- Wants agent working artifacts (`.agent/` session logs, output/test logs, snapshots) kept out of the repository entirely: after confirming the directory isn't needed in the repo, gitignore it, delete it from disk, and purge every trace from git history — the public repo should carry only the single always-read `AGENTS.md` doc, with any essential roadmap/status content folded into it before the purge. Confidence: 0.85
- Treats deploy-readiness as an explicit checkpoint when assessing remaining work: before declaring the system "prepared for deploy," verifies the build succeeds in a fresh environment (no local generated files or secrets), and treats a fresh-build failure (e.g., Vercel's `prisma generate` choking on a missing `DATABASE_URL`) as a P0 blocker to fix immediately rather than deferring it. Confidence: 0.7
- Prefers keeping the client build self-contained and decoupled from server-generated code: mirrors Prisma/domain enums as local client types instead of importing from the server package, so a static client deploy never needs `prisma generate` or database credentials at build time (removes the cross-workspace build coupling entirely). Confidence: 0.65
- Wants marketing/landing statistics and copy to be real, verifiable, and honestly sourced : unverifiable or generic-sounding figures (e.g., a counsellor ratio that can't be tied to a named source) get swapped for citable ones, numbers corrected against primary sources (1.94M → 1.84M UTME candidates), and source lines state exactly what backs the figures without overclaiming. Confidence: 0.65
- Prefers landing-page stat cards presented as a tight, professional data strip (compact `tabular-nums` values, subtle divider rules, small captions) rather than oversized bold numbers that read as unprofessional. Confidence: 0.55
- Prefers the hero/landing content to be short and scannable: stat cards in the hero carry only a compact teaser (stat + short label), and the long explanatory detail is moved into a dedicated section below the hero (e.g., a "Why StreamWise?" section answering the why question) rather than letting the hero say too much. Confidence: 0.7
- Multi-column landing/marketing grids (hero stat cards, feature cards) must stack into a single column on mobile/tablet instead of compressing their columns into cramped space: always pair a grid (e.g., `grid-cols-3`) with a mobile-first fallback (e.g., `grid-cols-1 sm:grid-cols-3`) via Tailwind responsive prefixes, never a bare fixed column count. Confidence: 0.65

- Wants user-facing copy (landing/marketing prose, e.g., the "Why StreamWise?" section) to be grammatically correct and properly punctuated: fixes cover missing articles, subject-verb agreement, and consistent pronoun voice within a section (not mixing "you" into an otherwise third-person "students" argument); raw symbols and abbreviations in stats ("<30%", "1.8M+") are written out ("Less than 30%", "1.8 million+") because they read as rendering bugs and unpolished. Confidence: 0.7
- Maintains a `todo.txt` file (at repo root) that tracks the current work plan plus findings, always overwriting it with the most recent analysis/todos and updated/checked off items so progress is recoverable in the event of an IDE crash mid-task. Confidence: 0.75

- When a specific component is reported as the broken one (e.g., a user says "the nav was the main problem"), fix/verify that specific component first and focus the work there, rather than launching a broad multi-file pass that restyles every grid/table heuristics-across-the-app; the reported component is the real target, not a list of guessed secondary grids. Confidence: 0.6
- On mobile, the topbar/nav should not try to display every detail inline; it is expected to collapse into the hamburger/mobile drawer (facet `Navbar`'s `showMobileMenu` / drawer) which does "the heavy lifting," instead of cramming brand + subtitle + login + signup + theme toggle all inline. Confidence: 0.65

